<% if @map %>
  <%= gmaps({
         :map_options => { :auto_zoom => false, :zoom => 17, :type => "HYBRID"},
         :markers     => { :data => @map},
         :direction => {:data => {:to => @listing.address.location_string}}
        }
        )
  %>
  <%# , :options => { :raw => '{ animation: google.maps.Animation.BOUNCE }' }   %>
<% end %>