<h1>
  <%= current_college if current_college %> Student Apartment Communities
  <% if current_user && current_user.admin? %>
    <span class="new_link_holder"><%= link_to 'New Community', new_community_path, :class => 'add_link' %></span>
  <% end %>
</h1>

<%= render partial: "communities/table", locals: {communities: @communities} %>

<br />

<% if current_user && current_user.admin? %>
  <%= link_to 'New Community', new_community_path %>
<% end %>