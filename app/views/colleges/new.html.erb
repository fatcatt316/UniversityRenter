<h1>New college</h1>

<%= form_for(@college) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'colleges/form', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', colleges_path %>